# Plugin Config
update-checker: true
metrics: true

# Default block message if the command does not have a custom block message.
block-message: "&4Error: &fThis command is disabled, Sorry!"
isworldblacklist: false

# Blocked Command List
commands:
  example:
    allowed-worlds:
      - world
  example2:
    allowed-worlds:
      -world_nether