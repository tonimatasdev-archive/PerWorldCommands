name: PerWorldCommands
version: 1.0.5
author: TonimatasMC
prefix: PerWorldCommands
description: PerWorldCommands
api-version: 1.13

main: net.tonimatasmc.perworldcommands.PerWorldCommands

commands:
  pwc:
    description: Primary command.
  perworldcommands:
    description: Primary command.

permissions:
  pwc.cmd:
    description: PerWorldCommands primary permission.

  perworldcommands.cmd:
    description: PerWorldCommands primary permission.